{
  "endPointHostname": "${MINIO_ENDPOINT_HOSTNAME}",
  "virtualPath": "",
  "secretKey": "${MINIO_SECRET_KEY}",
  "accessKey": "${MINIO_ACCESS_KEY}",
  "partSize": "${MINIO_PART_SIZE}",
  "name": "minio",
  "description": "The S3 endpoint on a local Minio container for testing",
  "endPointPort": "${MINIO_ENDPOINT_PORT}",
  "endPointSecure": false,
  "region": "${MINIO_REGION}"
}