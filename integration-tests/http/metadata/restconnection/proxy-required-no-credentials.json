{
  "name": "proxy-required-no-credentials",
  "auth_type": "No Auth",
  "base_url": "http://${PROXY_ONLY_HOST}",
  "test_url": "",
  "ignoreSsl": false,
  "proxy_host": "${PROXY_HOST}",
  "proxy_port": "${PROXY_AUTH_PORT}"
}
