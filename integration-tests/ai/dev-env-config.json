{
  "variables": [
    {
      "name": "OLLAMA_BASE_URL",
      "value": "http://ollama:11434",
      "description": "Ollama endpoint on the docker integration-test network. The ai-provider metadata objects point here, so this is for pipelines that need the URL directly."
    }
  ]
}
